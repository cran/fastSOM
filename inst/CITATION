citHeader("To cite SOM in publications use")

## R >= 2.8.0 passes package metadata to citation().
if(!exists("meta") || is.null(meta)) meta <- packageDescription("SOM")
year <- sub("-.*", "", meta$Date)
note <- sprintf("R package version %s", meta$Version)

citEntry(entry = "Manual",
         title = "{SOM}: Calculation of Spillover Measures",
         author = c(person("Stefan","Kl\\\"o{\\ss}ner"),person("Sven","Wagner")),
         year = year,
         note = note,
         url = "http://CRAN.R-project.org/package=SOM",

         textVersion =
         paste("Stefan Kloessner","Sven Wagner",
   	       sprintf("(%s).", year),	 
               "SOM: Calculation of Spillover Measures.",
	       paste(note, ".", sep = ""),
               "URL http://CRAN.R-project.org/package=SOM")
)
